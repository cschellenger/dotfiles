echo "initializing with $HOME/.bash_container"
export GOROOT=/usr/local/go
if ! [[ "$PATH" =~ ":GOROOT/bin" ]]; then
    PATH="$PATH:$GOROOT/bin"
fi
export PATH
export EDITOR=emacs
alias stow="stow -t ~ -d ~/dev/dotfiles"
